.DS_Store

# NPM
node_modules/

# Dev
dev/
dev_firefox/
dev_safari/

# Prod
dist/
dist_firefox/
dist_safari/
builds/
changelogs/
assets/shared

# Xcode
*.xcuserstate
*.xcuserdatad

.context
.cursorrules
.env
context/

# Local debugging notes/logs
.cursor/debug-*.log
.cursor/feishu-image-debug-notes.md

# Chrome extension signing key (do not commit)
keys/

# Per-feature worktrees (superpowers workflow) — never tracked
.claude/worktrees/

# Playwright persistent profiles for sites that require interactive QR login
# (scys-pw-profile is created by scripts/scys-login-persist.ts after a one-time
# WeChat scan; subsequent e2e runs reuse it via runRealClip({ userDataDir }))
.*-pw-profile/

# Local Claude Code chat-history exports — never tracked
.claude/chat-exports/

# Serena MCP local project workspace (config + LSP symbol cache) — never tracked
.serena/

# Superpowers work area — 2026-05-27 翻转为正向黑名单 ([[feedback_specs_plans_local_only]]):
# 默认 track docs/superpowers/{specs,plans,BACKLOG.md}（spec/plan/BACKLOG 有协作 + 复盘价值），
# 仅以下两类继续 ignore：
#   - feishu.md：飞书 OpenAPI 凭证（[[reference_feishu_credentials]]）
#   - test-reports/：audit 审计中间产物（每次跑都变、本地一次性）
docs/superpowers/feishu.md
docs/superpowers/test-reports/

# macOS Vision OCR compiled binary (build with `swiftc -O scripts/macos-vision-ocr.swift -o scripts/macos-vision-ocr`)
scripts/macos-vision-ocr
